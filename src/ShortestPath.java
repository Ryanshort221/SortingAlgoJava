
public class ShortestPath {

    // public int minDistance(int[] dist, boolean[] visited) {
    //     int min = Integer.MAX_VALUE, index = -1;
    //     for (int z = 0; z < dist.length; z++) {
    //         if (visited[z] == false && dist[z] <= min) {
    //             min = dist[z];
    //             index = z;
    //         }
    //     }
    //     return index;
    // }
    // public void dijkstra(int graph[][], int startNode) {
    //     int dist[] = new int[graph.length];
    //     boolean visited[] = new boolean[graph.length];

    //     for (int i = 0; i < graph.length; i++) {
    //         dist[i] = Integer.MAX_VALUE;
    //         visited[i] = false;
    //     }
    //     dist[0] = 0;
    //     for (int j = 0; j < graph.length; j++) {
    //         int min = minDistance(dist, visited);
    //         visited[min] = true;
    //         for (int x = 0; x < graph.length; x++) {
    //             if (!visited[x] && graph[x][v] != 0 && dist[min] != Integer.MAX_VALUE && dist)  
    //         }
    //     }
}
